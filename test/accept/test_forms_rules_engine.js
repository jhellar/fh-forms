var mongoose = require('mongoose');
var async = require('async');
var util = require('util');

var assert = require('assert');

module.exports.testBasicFormRules = function (finish) {

  assert.fail("must implement test");
  
};

